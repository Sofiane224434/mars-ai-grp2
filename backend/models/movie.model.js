import { query } from '../config/db.js';

const Movie = {
    async createMovie({ }) {
        const sql = ``;
        //étape 1: remplir la table movie
        //étape 2: utiliser l'id obtenu pour remplir la table director
        //const result = await query(sql, [myvalueshere]);

        //get id
        //const myid = result.id;
    }
}